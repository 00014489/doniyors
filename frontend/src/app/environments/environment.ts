export const environment = {
  production: false,

  /**
   * Absolute URL of the API during development, because `ng serve` and the
   * API run on different origins. Replaced at build time by
   * `environment.prod.ts` — see `fileReplacements` in angular.json.
   */
  apiUrl: 'https://visual-ant-shed-ellis.trycloudflare.com/api',
};
